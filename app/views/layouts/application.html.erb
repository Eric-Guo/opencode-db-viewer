<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <%= display_meta_tags site: "OpenCode DB Viewer", separator: "-", reverse: true %>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_pack_tag "application" %>
    <%= javascript_pack_tag "application" %>
    <%= yield :head %>
  </head>
  <body class="overflow-x-hidden" data-controller="coreui theme">
    <%= render "layouts/sidebars/#{@_sidebar_name}" unless @_sidebar_name.nil? -%>
    <div class="<%= @_wrapper_class.join(" ") unless @_wrapper_class.nil? -%>"
         <%= tag.attributes(data: {
           controller: "project-search",
           project_search_url_value: projects_path(format: :json),
           project_search_empty_value: t("viewer.no_projects"),
           project_search_error_value: t("viewer.suggestions_unavailable"),
           project_search_hint_value: t("viewer.project_search_hint")
         }) if @_sidebar_name %>>
      <% if @_sidebar_name.nil? %>
        <%= yield %>
      <% else %>
        <%= render partial: 'layouts/header', locals: {} %>
        <% if content_for?(:action_bar) %>
          <div class="action_bar">
            <%= yield :action_bar %>
          </div>
        <% end %>
        <div class="body flex-grow-1 px-2 py-3">
          <div class="<%= @_container_class -%>">
            <% unless @skip_title %>
              <div class="fs-3 fw-semibold"><%= title -%></div>
            <% end %>
            <%= render partial: "layouts/breadcrumb", locals: { breadcrumbs: @_breadcrumbs } if @_breadcrumbs.present? %>
            <%= yield %>
          </div>
        </div>
        <%= render "layouts/project_search" %>
        <div class="modal fade" id="coreuiModal" tabindex="-1" aria-hidden="true"></div>
        <%= footer %>
      <% end %>
    </div>
  </body>
</html>
